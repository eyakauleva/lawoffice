spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=law_office_schema
    username: postgres
    password: system
  sql:
    init:
      mode: always
  jackson:
    default-property-inclusion: non_null

mybatis:
  mapper-locations: "classpath:mapper/**/*-mapper.xml"